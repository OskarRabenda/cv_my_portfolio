# Agent instructions

These instructions apply to any AI agent working in this repository.

## Git workflow

- After every change, commit it with a simple, concise message.
- Do **not** add yourself (the agent) as a co-author. No `Co-Authored-By` trailer.
- After committing, push to the `main` branch.
